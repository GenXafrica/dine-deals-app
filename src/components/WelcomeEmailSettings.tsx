import React from 'react';

/**
 * Obsolete welcome email settings component.
 * Live welcome emails are queued by database triggers and sent by the shared worker.
 *
 * This file is kept as a safe no-op placeholder only so any old reference
 * will not break the build.
 */
export default function WelcomeEmailSettings() {
  return null;
}