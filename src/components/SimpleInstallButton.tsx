import React from 'react';

/**
 * Obsolete simple install button.
 * Live install flow is handled elsewhere.
 *
 * This file is kept as a safe no-op placeholder only so any old reference
 * will not break the build.
 */
export const SimpleInstallButton: React.FC = () => {
  return null;
};

export default SimpleInstallButton;